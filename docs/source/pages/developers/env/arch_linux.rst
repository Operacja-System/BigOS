Arch Linux
==========

To install all necessary packages run:

    .. code-block:: bash

        sudo pacman -Sy --needed riscv64-linux-gnu-gcc dosfstools e2fsprogs mtools qemu-system-riscv cmake clang-tools-extra clang
